<!DOCTYPE html>
<html>
    <head>
        
        
    </head>
    <body>
        <h3>You have recieved a message from the SMB IT's contact form, from <strong><%= "#{ @first_name} #{@last_name}." %></strong></h3>
        <h4>Business: <%= "#{@business_name}" %></h4>
        <h4>Email: <%= "#{@email}" %></h4>
        <h4>Phone: <%= "#{@phone}" %></h4>
        <h4>Email: <%= "#{@email}" %></h4>
        <p>Question 1: <%= "#{@question1}" %></p>
        <p>Question 2: <%= "#{@question2}" %></p>
    </body>
</html>